import styled from 'styled-components';

export const Container = styled.label`
  display: flex;
  flex-direction: column;
  gap: 3px;

  input {
    background: #fff;
    border-radius: 5px;
    border: 0;
    padding: 8px 16px;
    font-size: 16px;
  }
`;
